type user = {
  readonly name: string;
  age: number;
  skill?: boolean;
};

interface Devs {
  readonly name: string;
  age: number;
  skill?: boolean;
};

const devs: Devs = {
  name: "mahir siam",
  age: 30,
};
